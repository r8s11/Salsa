{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "*.{jpg,gif,png}",
      "assets/*"
    ]
  },
  "routes": [
    {
      "route": "/calendar",
      "rewrite": "/calendar"
    },
    {
      "route": "/submit",
      "rewrite": "/submit"
    }
  ]
}