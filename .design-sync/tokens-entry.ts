// Tokens-only entry for design-sync.
// Salsa Segura is a web app, not a component library.
// All components are excluded via componentSrcMap in config.json.
// The design system value is the Ritmo Vivo CSS tokens in styles-with-fonts.css.
export {};
